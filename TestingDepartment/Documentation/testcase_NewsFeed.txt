NEWSFEED
For the NewsFeed we will test if it shows the latest news:
-getnews()-Check if the news alerts contain all the news from the specified day;
		possible errors:fail to send the news;
				news alert is empty;
				news aren't filtred by the NewsFilter
-check_NG()-Check the connection with NewsGetter;
		possible errors:fail to connect;
				the NewsGeter send wrong answer or send a empty list;
-check_NF()-Check the connection with NewsFilter 
		possible errors:fail to connect; 
				the NewsFilter send duplicate news;
-check_C-Check the connection with Controller 
		possible errors:fail to connect;
				send the wrong answer to one comad;